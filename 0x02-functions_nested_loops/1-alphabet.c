#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void print_alphabet(void)
{
        char i;
        char s = '\n';
        for (i = 'a'; i <= 'z'; i++)
                _putchar(i);
        _putchar(s);
}
